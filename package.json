{
  "name": "devops-quality-demo",
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint .",
    "test": "jest --coverage",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run"
  },
  "devDependencies": {
    "eslint": "^8.50.0",
    "jest": "^29.0.0",
    "cypress": "^12.0.0"
  }
}
